services:
  server:
    environment:
      - POSTGRES_HOST=db
      - MINIO_ENDPOINT=minio:9000
      - REDIS_HOST=redis
      - MINIO_SERVER_URL=http://localhost:9000
      - TYPST_SERVICE_ADDR=typst:9090