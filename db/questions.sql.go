// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.31.1
// source: questions.sql

package db

import (
	"context"

	"github.com/google/uuid"
)

const createQuestion = `-- name: CreateQuestion :one
INSERT INTO questions (id, quiz_id, text, type, options, correct_answer, explanation, points, order_index)
VALUES ($1, $2, $3, $4, $5, $6, $7, $8, $9)
RETURNING id, quiz_id, text, type, options, correct_answer, explanation, points, order_index
`

type CreateQuestionParams struct {
	ID            uuid.UUID    `json:"id"`
	QuizID        uuid.UUID    `json:"quiz_id"`
	Text          string       `json:"text"`
	Type          QuestionType `json:"type"`
	Options       []byte       `json:"options"`
	CorrectAnswer string       `json:"correct_answer"`
	Explanation   string       `json:"explanation"`
	Points        int          `json:"points"`
	OrderIndex    int          `json:"order_index"`
}

func (q *Queries) CreateQuestion(ctx context.Context, arg CreateQuestionParams) (Question, error) {
	row := q.db.QueryRow(ctx, createQuestion,
		arg.ID,
		arg.QuizID,
		arg.Text,
		arg.Type,
		arg.Options,
		arg.CorrectAnswer,
		arg.Explanation,
		arg.Points,
		arg.OrderIndex,
	)
	var i Question
	err := row.Scan(
		&i.ID,
		&i.QuizID,
		&i.Text,
		&i.Type,
		&i.Options,
		&i.CorrectAnswer,
		&i.Explanation,
		&i.Points,
		&i.OrderIndex,
	)
	return i, err
}

const getQuestionsByQuizID = `-- name: GetQuestionsByQuizID :many
SELECT id, quiz_id, text, type, options, correct_answer, explanation, points, order_index FROM questions WHERE quiz_id = $1 ORDER BY order_index ASC
`

func (q *Queries) GetQuestionsByQuizID(ctx context.Context, quizID uuid.UUID) ([]Question, error) {
	rows, err := q.db.Query(ctx, getQuestionsByQuizID, quizID)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	items := []Question{}
	for rows.Next() {
		var i Question
		if err := rows.Scan(
			&i.ID,
			&i.QuizID,
			&i.Text,
			&i.Type,
			&i.Options,
			&i.CorrectAnswer,
			&i.Explanation,
			&i.Points,
			&i.OrderIndex,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}
